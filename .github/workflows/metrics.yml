name: Generate Metrics

on:
  schedule:
    - cron: "0 0 * * *"  # Daily
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          user: daneil-k
          template: classic
          base: header, activity, community, repositories, metadata
          config_timezone: Asia/Kolkata
          plugin_isocalendar: yes
          plugin_isocalendar_duration: half-year
          plugin_languages: yes
          plugin_languages_colors: github
          plugin_languages_threshold: 0%
          plugin_stars: yes
          plugin_stars_limit: 4
```

## 🚀 Complete Folder Structure
```
daneil-k/
├── .github/
│   └── workflows/
│       ├── snake.yml
│       ├── update-activity.yml
│       └── metrics.yml
├── assets/
│   └── (optional: add custom images here)
└── README.md
```

## ✨ What's New & Animated

### Added Animations:
1. ✅ **Gradient Wave Headers** - Smooth color transitions
2. ✅ **Enhanced Typing Effect** - Multiple rotating messages
3. ✅ **Animated Rainbow Dividers** - Colorful separators
4. ✅ **GIF Icons** - Throughout the profile
5. ✅ **Animated Progress Bars** - Visual skill indicators
6. ✅ **Contribution Snake** - Eats your commits
7. ✅ **Activity Graph** - Smooth gradient visualization
8. ✅ **Trophy Animations** - Achievement showcase
9. ✅ **Rotating Dev Quotes** - Changes daily
10. ✅ **Pulsing Badges** - All social links
11. ✅ **Animated Footer** - Twinkling wave effect
12. ✅ **Profile View Counter** - Real-time updates
13. ✅ **Follower/Star Counters** - Live metrics
14. ✅ **Recent Activity Feed** - Auto-updates every 2 hours

## 🎯 Setup Instructions

### Step 1: Create All Workflow Files

1. Go to your `daneil-k` repository
2. Create each workflow file:
   - Click **"Add file"** → **"Create new file"**
   - Name: `.github/workflows/snake.yml`
   - Paste the snake workflow code
   - Commit
   - Repeat for `update-activity.yml` and `metrics.yml`

### Step 2: Update README

1. Click on `README.md`
2. Edit and paste the new enhanced version
3. Update your personal info:
   - Email address
   - LinkedIn URL
   - Twitter handle
   - Discord server
   - Telegram username

### Step 3: Trigger All Workflows

1. Go to **"Actions"** tab
2. Run each workflow manually:
   - "Generate Animated Snake"
   - "Update Recent Activity"
   - "Generate Metrics"
3. Wait 2-3 minutes for completion

### Step 4: Enable GitHub Pages (for metrics)

1. Go to **Settings** → **Pages**
2. Source: **Deploy from a branch**
3. Branch: **output** → **/root**
4. Save

## 🎨 Color Customization

Want different colors? Change these values:

**Purple Theme (Current):**
- `color=9370DB` (Medium Purple)
- `theme=midnight-purple`

**Blue Theme:**
```
color=58a6ff
theme=github-dark
```

**Red/Pink Theme:**
```
color=FF6B6B
theme=radical
```

**Green Theme:**
```
color=00FF00
theme=algolia
